# EcoSphere environment configuration — copy to .env and fill in.
# NEVER commit the real .env file (it is gitignored).

# Optional: AI Copilot LLM API key (Phase 6 feature — core platform works without it)
ECOSPHERE_LLM_API_KEY=

# Database connection (fill in once the stack is decided)
DB_HOST=localhost
DB_PORT=
DB_USER=
DB_PASSWORD=
