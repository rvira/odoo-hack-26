target-version = "py311"

[lint.per-file-ignores]
# Odoo loads packages via side-effect imports in __init__.py — not unused.
"__init__.py" = ["F401"]

[format]
# Odoo manifests conventionally keep single quotes (OCA style).
exclude = ["**/__manifest__.py"]
